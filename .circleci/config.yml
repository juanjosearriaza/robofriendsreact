version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:14.10.1 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "installing" # run the `echo` command
      - run: npm install
      - run: CI=true npm run build
  test:
    docker:
      - image: cimg/node:14.10.1 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "testing" # run the `echo` command
      - run: npm run test
  hithere:
    docker:
      - image: cimg/node:14.10.1 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "helllooo¡¡¡" # run the `echo` command
workflows:
  version: 2.1
  bobby_sally:
    jobs:
      - build
      - hithere
      - test:
          requires:
            - hithere
